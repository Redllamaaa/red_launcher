{
  "name": "red_launcher",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node build-html.js && vite",
    "build": "node build-html.js && tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-opener": "^2",
    "ejs": "^6.0.1",
    "github-syntax-dark": "^0.5.0",
    "jquery": "^4.0.0",
    "lodash.merge": "^4.6.2",
    "toml": "^5.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~6.0.3",
    "vite": "^8.0.16"
  }
}
